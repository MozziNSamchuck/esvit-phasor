MODEL:
  NAME: swin_transformer
  SPEC:
    IN_CHANS: 2
    PATCH_SIZE: 4
    DIM_EMBED: 96
    DEPTHS: [2, 2, 6, 2]
    NUM_HEADS: [3, 6, 12, 24]
    WINDOW_SIZE: 4
    MLP_RATIO: 4.0
    QKV_BIAS: True
    DROP_RATE: 0.0
    ATTN_DROP_RATE: 0.0
    DROP_PATH_RATE: 0.2
    USE_APE: False
    PATCH_NORM: True
TRAIN:
  LR_SCHEDULER:
    METHOD: cosine
